function createWindow(file) {
    window.outgoing.createWindow(file);
}

function sendNotification(title, body) {
    window.outgoing.sendNotification(title, body);
}

function searchSeed(info) {
    window.outgoing.searchSeed(info);
}